spring:
  datasource:
    url: jdbc:mysql://129.28.166.134/letter2future?useUnicode=true&characterEncoding=utf-8&serverTimezone=UTC&useSSL=true
    username: letter2future
    password: whoIsNo.1
    driver-class-name: com.mysql.cj.jdbc.Driver
    druid:
      initial-size: 3
      min-idle: 5
      max-active: 20
      max-wait: 30000
      time-between-eviction-runs-millis: 60000
      min-evictable-idle-time-millis: 300000
      validation-query: SELECT 1 FROM DUAL
      test-while-idle: true
      test-on-borrow: false
      test-on-return: false

